Faca uma algoritmo para ler um valor N qualquer e em seguida ler N números inteiros. No final, o seu algoritmo deve informar a quantidade de números multiplos de 12.

    COM PARA

Algoritmo multiplo_12;
Variaveis
    i, num, multiplo, cont : Inteiro;
Inicio
    cont <- 0;
    Escreva("Escreva a quantidade de números que deseja saber se são múltiplos de 12: ");
    Leia(num);
    Se num > 0 Entao
        Para i <- 1 Ate num Faca
	Inicio
	    Escreva("Digite um número: ");
	    Leia(multiplo);
	        Se Resto(multiplo, 12) == 0 Entao
	            cont <- cont + 1;
	FimPara
    Escreva("Dos números escolhidos" + cont + "deles são múltiplos de 12");
    Senao
	Escreva("Não há números a calcular");
Fim.

    COM ENQUANTO

    Algoritmo multiplo_12;
Variaveis
    i, num, multiplo, cont : Inteiro;
Inicio
    cont <- 0;
    i <- 1;
    Escreva("Escreva a quantidade de números que deseja saber se são múltiplos de 12: ");
    Leia(num);
    Se num > 0 Entao
        Enquanto i <= num Faca
	Inicio
	    Escreva("Digite um número: ");
	    Leia(multiplo);
	        Se Resto(multiplo, 12) == 0 Entao
	            cont <- cont + 1;
	    i <- i + 1;
	FimEnquanto
    Escreva("Dos números escolhidos" + cont + "deles são múltiplos de 12");
    Senao
	Escreva("Não há números a calcular");
Fim.