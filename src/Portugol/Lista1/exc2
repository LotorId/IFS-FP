Desenvolva um programa que leia um número inteiro e exiba o resultado do
quadrado deste número.

Algoritmo quadrado;
Variaveis
    n, q : Inteiro;
Inteiro
    Escreva("Entre com um número para descobrir seu quadrado: ");
    leia(n);
    q <- n * n;
    Escreva("O quadrado de " + n + "é: " + q);
Fim.